# Configuration for archive-title-dedup.
# Run with: archive-title-dedup -report -config config.yaml

# AWS region the table lives in.
region: us-east-1

# Archive table to scan for records with non-unique titles.
table_name: Archive-bxbkjhe235e3jcwcjcji5txvlm-vtdlpdev

# Directory the report is written to (created if missing).
output_dir: output

# Report file name inside output_dir. Defaults to duplicate_titles.json.
output_file: duplicate_titles.json

# Number of parallel scan segments; each is scanned by its own goroutine.
concurrency: 10
